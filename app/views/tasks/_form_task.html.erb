<%= simple_form_for(task) do |f| %>
    <div class="form-inputs">
      <%= f.input :category, collection: Task::TASK_CATEGORIES %>
      <%= f.input :description, input_html: { :maxlength => 60 }%>
    </div>
    <div class="form-actions">
      <%= f.button :submit, class: "btn btn-primary" %>
    </div>
<% end %>
