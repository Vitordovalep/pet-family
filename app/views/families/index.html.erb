<div class="container">
  <h1>Famílias</h1>
  <h2>sua família: <%= current_user.family.name %></h2>

  <% @families.each do |family| %>
    <li><%= link_to family.name, family_path(family)  %></li>
  <% end %>
</div>
